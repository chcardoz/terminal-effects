{
  "name": "terminal-effects-renderer",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "build:runtime": "scripts/build-runtime.sh",
    "typecheck": "pnpm -r typecheck"
  },
  "packageManager": "pnpm@10.13.1",
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
